# M4.5 Completion Summary — DCM Pulse Framework (D-137)

**Date:** March 15, 2026
**Tests:** 509 passing (was 501 at M4 close)
**Pure Balance regression:** 62 (was 65)

---

## What Changed

- **DCM trigger:** Grain-free + 3+ legumes in top 7 (D-013) replaced with positional pulse load detection (D-137)
- **3-rule OR:** Heavyweight (pulse in top 3), Density (2+ pulses in top 10), Substitution (pulse protein isolate in top 10)
- **Potatoes exonerated:** Removed from DCM trigger and Heart Risk concern tag membership
- **Grain-free gate removed:** Grain-inclusive pulse-heavy diets now correctly flagged
- **New DB flags:** `is_pulse` and `is_pulse_protein` on `ingredients_dict` (Migration 009)
- **Penalty unchanged:** -8% (x0.92) DCM, +3% (x1.03) mitigation (taurine + L-carnitine)

## Files Created

| File | Purpose |
|------|---------|
| `supabase/migrations/009_m45_pulse_flags.sql` | Add `is_pulse` + `is_pulse_protein` columns |
| `scripts/data/backfill_pulse_flags.ts` | Classify pulse ingredients via exact match + patterns |
| `M4.5-SUMMARY.md` | This file |

## Files Modified

| File | Change |
|------|--------|
| `src/types/scoring.ts` | Added `is_pulse`, `is_pulse_protein`, `DcmRuleName`, `DcmResult` |
| `src/types/index.ts` | Added `is_pulse`, `is_pulse_protein` to `IngredientDict` |
| `src/services/scoring/speciesRules.ts` | Replaced D-013 DCM with D-137 3-rule OR via `evaluateDcmRisk()` |
| `src/services/scoring/pipeline.ts` | Hydrate `is_pulse`/`is_pulse_protein` from DB |
| `src/components/DcmAdvisoryCard.tsx` | Rule-specific copy with mechanism citations |
| `src/components/ConcernTags.tsx` | Heart Risk fires on `dcmFires` prop, potatoes removed |
| `src/screens/ResultScreen.tsx` | Pass `DcmResult` to card, `dcmFires` to tags, removed grain-free gate |
| `CLAUDE.md` | Phase update, self-check items |
| `DECISIONS.md` | D-013 marked SUPERSEDED, D-137 appended |
| `ROADMAP.md` | M4.5 marked complete |
| 10 test files | Added `is_pulse`/`is_pulse_protein` defaults to `makeIngredient` helpers |

## Test Coverage (D-137 specific)

- 11 DCM rule tests in `speciesRules.test.ts` (3 rules + exclusions + mitigation + grain-free gate removal)
- Pure Balance regression updated in `engine.test.ts`, `realDataTrace.test.ts`, `allergenOverride.test.ts`
- Explicit potato/soy exclusion tests
- Grain-inclusive pulse-heavy diet test

## M5 Dependencies

- `is_pulse` flag available for pantry diet analysis and Safe Swap filtering
- `DcmResult.triggeredRules` available for Kiba Index correlation (M8+)
- `evaluateDcmRisk()` exported for any consumer (ResultScreen, concern tags, future analytics)
