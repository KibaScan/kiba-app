# Project Status — Last updated 2026-04-04 (session 20)

## Active Milestone
**M9 — UI Polish & Search** (search UX overhaul, general polish, UX friction fixes)

## Last Completed
**M8 — Kiba Index** (April 1, 2026, branch `m5-complete`)

## What Works
- Scan-to-score pipeline: barcode → 3-layer scoring (IQ/NP/FC) → result screen
- 19,058 products (v7 reimport, Chewy + Amazon + Walmart)
- Pet profiles, pantry (auto-deplete, budget-aware servings), treat battery
- Appointments (CRUD, recurring, reminders, health records)
- Push notifications (feeding, low stock, empty, recall, appointment, medication, weight estimate, digest)
- HomeScreen v2, Community tab, Top Matches, RevenueCat paywall
- **Health condition scoring** — 12 conditions (P0-P3), Layer 3 adjustments, cardiac+DCM zero-out
- **Health condition UI** — expanded condition picker (mutual exclusions, sub-types), ResultScreen advisories, medication tracking
- **Weight goal slider (D-160)** — 7-position discrete slider (-3 to +3), cat -3 absent, condition-blocked positions, premium-gated, auto-reset on conflict
- **Caloric accumulator (D-161)** — daily delta tracking in auto-deplete cron, weight estimate push notifications, WeightEstimateSheet (confirm/enter/dismiss), PetHubScreen banner
- **BCS reference (D-162)** — 9-point educational guide, species tabs, tappable selection saves to pet profile, cat primordial pouch callout, free for all users
- **Compare flow** — 9-rule key differences engine, two-column CompareScreen (score breakdown, nutrition table, ingredients), CompareProductPickerSheet (search, recent scans, camera), kcal/cup estimation fallback (DB → kcal/kg × 110g → Atwater), PortionCard kcal/cup display. **"Your Other Pets" section** — collapsible, lazy-loaded scores for other same-species pets on CompareScreen
- **Vet Report PDF** — 4-page diet-centric report via expo-print (no Kiba scores). Pet profile with BCS gauge, caloric summary, combined nutrition with AAFCO checks, supplemental nutrients, flags, weight tracking, per-product detail, condition management notes, owner dietary cards (28 cards × conflict detection), vet notes. Premium-gated via `canExportVetReport()`.
- **Safe Swap curated layout (Plan 2)** — daily dry food gets curated 3-pick (Top Pick / Fish-Based / Great Value). Fish-Based uses `allergen_group = 'fish'` from `ingredients_dict` (not regex). Great Value uses `price / product_size_kg` (migration 023). Fish allergy → Fish-Based replaced with "Another Pick" (2nd highest score). Falls back to generic top-3 if < 2 curated slots fill. All other categories unchanged (generic top-3).
- **Safe Swap simplified** — removed multi-pet chip row, active pet only. Collapsible (default closed), free users see inline premium CTA. Life stage hard filter. Great Value fallback to "Another Pick" when no price data.
- **Batch scoring — Approach F (Delta Scoring + Two-Phase Edge)** — `batchScoreHybrid()` tries Edge Function first (1000 products, two-phase), falls back to client-side (200 products). **Delta scoring:** checks cache maturity (≥80% of products scored) → only fetches new/updated products (near-instant). **Two-Phase Edge:** Phase 1 scores 200 products synchronously (~10s), Phase 2 scores remaining ~800 in background via `EdgeRuntime.waitUntil()` (chunked at 200 with 50ms GC yields). **Cache maturity check:** prevents "delta trap" when Phase 2 fails — triggers healing full batch on next scan. **Per-category rate limit** (scoring treats no longer blocks scoring dry food). All product queries ordered by `updated_at DESC`. Architecture doc: `docs/references/batch-scoring-architecture.md`.
- **Safe Swap filter hardening** — `CANDIDATE_POOL_SIZE` raised from 50 to 300 (6x buffer for exclusion filters). Exclusion queries chunked at 100 IDs (prevents 414 URI Too Long). `fetchBasePool()` filters by cached `is_supplemental` from `pet_product_scores` (D-146 aware, not raw DB value). Supplement category guard (`category = 'supplement'` → excluded). Runtime `isSupplementalByName()` safety net catches future data gaps. `tagFishBased()` also chunked.
- **Supplemental data patch (migration 024)** — 124 products flagged `is_supplemental = true` (toppers, mixers, lickables, sprinkles missed by enrichment pipeline). 2 oil products recategorized from `daily_food` to `supplement` (Pure Balance Salmon Oil, Raw Paws Coconut Oil). AAFCO cleared for supplemental products. Stale `pet_product_scores` cache invalidated.
- **Supplemental classifier expanded** — added `/topping/i` and `/lickable/i` to `SUPPLEMENTAL_NAME_PATTERNS` in `supplementalClassifier.ts`.
- **Price backfill** — 15,781 products updated with price + product_size_kg from v7 dataset. 74.6% of daily food now has price data for Great Value slots.
- **Affiliate link infrastructure (dormant)** — `AffiliateBuyButtons` component on ResultScreen (between PortionCard and Compare button). PantryCard "Reorder" button on low-stock items. `affiliateService.ts` generates Chewy/Amazon URLs from `source_url`/`chewy_sku`/`asin`/`affiliate_links` JSONB. D-020 compliant (zero scoring imports, buttons hidden when score < 50). D-053 compliant (Chewy shows estimated price, Amazon hides price). Config: `enabled: false` — flip on after affiliate program enrollment.
- **Condition-aware feeding frequency** — auto-populate `feedings_per_day` based on pet health conditions when adding to pantry.
- **Safe Switch Guide (M7)** — guided 7-day (dogs) / 10-day (cats) food transition with daily mix ratios (75/25 → 50/50 → 25/75 → 100%). `SafeSwitchSetupScreen` (preview + start), `SafeSwitchDetailScreen` (daily command center with proportion bar, tummy check pills, vertical timeline). `SafeSwitchBanner` on PantryScreen (day ring + mix ratio) and HomeScreen (compact status card). Entry points: "Switch to this" on Safe Swap cards, "Find a replacement" on low-scoring (<60%) daily food PantryCards. Daily notifications (9 AM mix reminder, 7 PM tummy check nudge). Upset advisory (2+ consecutive "upset" logs → informational card, D-095 compliant, no auto-action). One active switch per pet enforced at DB level (partial unique index). Free feature (no paywall gate). Migration 025.
- **Vet diet data fix (migration 027)** — restored 483 `is_vet_diet` flags lost during v7 reimport. D-135 bypass operational again. `import_products.py` updated to map `_is_vet_diet`.
- **MeScreen overhaul** — unified "Medical Records" card (vaccines + dewormings chronological, micro-icons, top 3 truncation + "See All"). `MedicalRecordsScreen` full-screen timeline. `HealthRecordDetailSheet` for edit/delete. `updateHealthRecord()` + `deleteHealthRecord()` service functions. Appointments card: persistent "+ Schedule" and "See All" links. Medication rows: chevrons. Matte Premium visual polish (`cardSurface`, `hairlineBorder` tokens). Score accuracy bar gradient.
- **SwipeableRow component** — reusable swipe-to-reveal wrapper (`src/components/ui/SwipeableRow.tsx`). Swipe left → delete (with confirmation alert + `deleteConfirm()` haptic). Swipe right → edit. Applied to: health record rows, medication rows, appointment rows on PetHubScreen, MedicalRecordsScreen, MedicationsListScreen, and AppointmentsListScreen.
- **MedicationsListScreen** — full-screen medication list with Current/Past segmented tabs, SwipeableRow (delete + edit), status dots. Access: PetHubScreen Medications card → chevron or "See All".
- **PetHubScreen card reorder** — Health Conditions → Appointments → Medications → Medical Records. All three data cards follow consistent pattern: title+chevron header, truncated list (max 3), "See All" link, bottom-anchored add CTA.
- **Medication reminders + duration** — MedicationFormScreen: up to 4 daily reminder times via DateTimePicker, duration presets (7/14/30/90 days + Ongoing + custom). `medicationNotificationScheduler.ts` (full-resync pattern, groups same-time reminders, skips expired meds). `medication_reminder` notification type + tap routing. NotificationPreferencesScreen toggle. Migration 028 (`reminder_times TEXT[]`, `duration_days INTEGER`, `medication_reminders_enabled BOOLEAN`).
- **Matte Premium design system** — `.agent/design.md` established. Tokens, card anatomy, typography, spacing, bottom sheet specs, anti-patterns, screen polish checklist. Referenced in CLAUDE.md spec table — read before touching any screen UI.
- **Card contrast alignment** — `Colors.cardSurface` bumped `#1C1C1E` → `#242424`, `Colors.hairlineBorder` bumped `rgba(255,255,255,0.08)` → `rgba(255,255,255,0.12)`. Matches legacy token contrast.
- **Category browse on HomeScreen** — 4 toggleable category cards (Daily Food, Toppers & Mixers, Treats, Supplements) with contextual sub-filter chips. Search bar filters by active category + sub-filter. Categories: Daily Food (Dry/Wet/Freeze-Dried/Vet Diet/Other), Toppers (Wet/Freeze-Dried/Dry), Treats (Crunchy & Biscuits/Jerky & Chews/Freeze-Dried/Lickables & Purees/Dental), Supplements (Joint & Hip/Skin & Coat/Digestive/Calming). `@shopify/flash-list` installed. Variety pack exclusion via `is_variety_pack` column (migration 029, ~1,706 flagged). `get_browse_counts` RPC for chip badge counts. `categoryBrowseService.ts` with cursor-based pagination. CategoryBrowseScreen exists but browse is inline on HomeScreen.
- **CompareScreen polish (Gemini review response)** — `getConversationalName()` utility produces short product references ("Feline Natural Chicken & Venison") for CTA buttons and `KeyDifferences` sentences instead of SEO-bloated full names. `KeyDifference` type restructured to `subject/verb/claim/trailing` with `buildDiff` helper auto-computing legacy `text` field; all 9 rules refactored to emit structured diffs rendered with bolded subject + claim. Product images framed on white 84px stage (`productImageStage`) mirroring `TopPicksCarousel` pattern. Score Breakdown heatmap: winner cyan, loser dimmed to `textSecondary`. Nutrition DMB uses bold-only differentiation (no color — respects D-094 clinical copy rule: "higher fat ≠ winner"). Center column labels dimmed to `textTertiary`. Ingredient list drops `1., 2., 3.` prefix (severity dot is sufficient ordering cue). Caption below product ring swapped from literal `{score}% match` to `getVerdictLabel()` ("Excellent match for Xhia") — matches `ResultScreen` pattern, no number echo, D-094 compliant. Global tab bar hidden on CompareScreen via `navigation.getParent().setOptions` pattern.
- **Ingredient canonical normalization (migration 030)** — fixes legacy v7 import rows where compound ingredient names were stored without underscore separators (`meatbyproducts` instead of `meat_by_products`, 6 known variants). Collision-safe: renames in place when no proper sibling exists, merges via `product_ingredients.ingredient_id` reassignment when both exist. Fixes a real functional bug: `ConcernTags.tsx` "Unnamed Source" pill silently failed to fire on affected products because its membership check used proper snake_case forms. Includes a discovery `DO $$` block that emits `RAISE NOTICE` for any additional jammed candidates not in the fix list. Invalidates `pet_product_scores` cache for affected products. `formatters.ts` `DISPLAY_NAME_OVERRIDES` map remains as defensive fallback. **Deployed session 20.**
- **Pantry SwipeableRow + legacy token migration (session 20, M9 polish)** — `PantryScreen` renderItem wraps `PantryCard` in `SwipeableRow`; swipe-left calls the existing `handleRemove(id)` (shared-item sheet + single-pet Alert branching preserved) and swipe-right calls `handleTap(id)` (routes to EditPantryItem or RecallDetail). `deleteConfirmMessage` intentionally omitted so the existing `handleRemove` flow owns confirmation — avoids double-confirm for single-pet items. `Colors.card` → `Colors.cardSurface` and `Colors.cardBorder` → `Colors.hairlineBorder` across `PantryCard.tsx`, `SafeSwitchBanner.tsx`, `AddToPantryStyles.ts`, `SharePantrySheet.tsx`, `PantryScreen.tsx`, `EditPantryItemScreen.tsx` (13 `card,` and 14 `cardBorder` occurrences). `cardSurface` is the same hex as legacy `card` (pure rename); `hairlineBorder` is `rgba(255,255,255,0.12)` vs `cardBorder`'s `#333333` — slightly softer. `PantryScreen.tsx:435` chip-background swap is flagged for visual review (the only non-border use of `cardBorder`).
- **Warmup one-liners (session 20)** — `CompareScreen.tsx:590` detects the `'your pet'` displayName fallback and passes `null` to `getVerdictLabel` (drops the trailing "for your" artifact). `keyDifferences.ts:169,183` colorant rule now uses `toDisplayName()` instead of the inline underscore-replace regex, so future `DISPLAY_NAME_OVERRIDES` entries for colorants flow through automatically. Note: CURRENT.md previously claimed ResultScreen.tsx:526 shared the same bug — verification showed ResultScreen already passes nullable `petName` correctly. Only CompareScreen needed the fix.
- **KeyDifference structured output snapshot (session 20)** — new Test 14 in `__tests__/utils/keyDifferences.test.ts` locks the `subject/verb/claim/trailing/text/icon/severity/affectedProduct/id` shape via a realistic multi-rule two-product comparison. Snapshot at `__tests__/utils/__snapshots__/keyDifferences.test.ts.snap`. Guards against accidental refactor of the 9 rule emitters or the `buildDiff` helper. Repo has no screen-level UI snapshot infrastructure (`react-test-renderer` not installed); this is the correct abstraction given the helper-extraction pattern convention.

## What's Broken / Known Issues
- **Stale browse scores**: CategoryBrowseScreen reads cached scores from `pet_product_scores` which can diverge from fresh ResultScreen scores (e.g. 82 vs 79) when pet profile changes after batch scoring. Root cause: batch scoring delta check counts ALL daily food for cache maturity but fetches by specific `product_form` — cache appears mature when dry/wet fill 80%, so freeze-dried (and other minority forms) never get scored. Workaround: fallback to unscored `products` query when scored cache is empty for a form. Long-term fix: make cache maturity check form-aware in both Edge Function and `batchScoreOnDevice.ts`.

## Numbers
- **Tests:** 1335 passing / 61 suites
- **Decisions:** 129 (D-001 through D-167, non-sequential, D-053 revised)
- **Migrations:** 30 (001–030)
- **Products:** 19,058 (483 vet diets, 1716 supplemental-flagged)

## Regression Anchors
- Pure Balance (Dog, daily food) = 62
- Temptations (Cat, treat) = 9
- Pure Balance + cardiac dog = 0 (DCM zero-out)
- Pure Balance + pancreatitis dog = 57 (fat >12% DMB penalty)

## Up Next
- **Visual QA on iOS simulator (session 19 + 20 changes)** — see checklist in Last Session block below. Covers Compare visual polish AND pantry swipe/token changes. User action: run dev build, walk the checklist.
- **PantryScreen.tsx:435 chip-background visual review** — the `cardBorder → hairlineBorder` swap at line 435 is the only non-border use (chip unselected state background). `rgba(255,255,255,0.12)` may read too faint vs `#333333`. If the unselected filter chips look broken or invisible, revert that single line to a dedicated chip-surface token.
- Legacy token migration across remaining screens (HomeScreen, ResultScreen, CompareScreen, MeScreen, etc.) — pantry is now done, other screens still use `Colors.card`/`Colors.cardBorder`
- Stale browse scores — CategoryBrowseScreen cache maturity check isn't form-aware (freeze-dried and other minority forms never get scored). Fix in both Edge Function and `batchScoreOnDevice.ts`
- Top Picks per category/sub-filter dedicated screen (stub ready in `categoryBrowseService.ts`)
- HomeScreen visual overhaul (custom assets, layout polish)
- Custom icon rollout (5 pending v2 bold variants)
- Search UX overhaul on HomeScreen
- Enroll in Chewy Affiliate Partners + Amazon Associates → flip `affiliateConfig.ts` enabled: true
- M10: Community points (XP engine, streaks, product submissions — lite scope)
- M11: Symptom Detective (deferred — major App Store update feature)

## Optimization Status
- **All cheatsheet sections complete:** S1–S13 (S9 N/A, S11/S14 pattern guidance only)
- **Maintenance guide complete:** scoring-details.md created, reference files audited, /audit-context + /milestone-close commands added
- **Hooks active:** protect-scoring.sh, regression-gate.sh, quality-gate.sh
- **Slash commands:** /boot, /handoff, /check-numbers, /audit-context, /milestone-close

## Last Session
- **Date:** 2026-04-04 (session 20)
- **Accomplished:** M9 polish batch — warmup one-liners + pantry polish + KeyDifference snapshot. Migration 030 deployment was completed before this session.
  - **Warmup A1 — `'your pet'` caption fix:** `src/screens/CompareScreen.tsx:590` now detects the `'your pet'` displayName fallback and passes `null` to `getVerdictLabel`, which drops the "for [name]" suffix entirely. `getVerdictLabel` at `constants.ts:111` already handles `null` correctly. **Important correction:** CURRENT.md previously claimed `ResultScreen.tsx:526` shared the same bug — verification showed it was fine. `ResultScreen.tsx:137` computes `petName = pet?.name ?? null` and passes that nullable value straight through. Only CompareScreen needed the fix.
  - **Warmup A2 — colorant rule title-casing:** `src/utils/keyDifferences.ts:169,183` colorant rule now uses `toDisplayName(i.canonical_name)` (already imported at line 9) instead of the inline `replace(/_/g, ' ').replace(/\b\w/g, ...)` regex. Future `DISPLAY_NAME_OVERRIDES` additions for colorants flow through automatically.
  - **Pantry SwipeableRow wiring (B2):** `src/screens/PantryScreen.tsx` imports `SwipeableRow` and wraps `<PantryCard>` in the `FlatList` `renderItem`. `onDelete={() => handleRemove(item.id)}` reuses the existing handler which already owns shared-item vs single-pet confirmation branching (multi-pet sheet vs Alert). `onEdit={() => handleTap(item.id)}` reuses the existing edit/navigate handler. `deleteConfirmMessage` is **intentionally omitted** — passing it would double-confirm for single-pet items because `handleRemove` shows its own Alert. Haptic fires slightly before the existing confirmation dialog; accepted as a tactile cue. Wrapper lives at render site (line ~471) matching the MedicalRecordsScreen / MedicationsListScreen / AppointmentsListScreen pattern — `PantryCard` itself is untouched by the swipe wiring.
  - **Pantry legacy token migration (B1):** `Colors.card` → `Colors.cardSurface` (13 occurrences) and `Colors.cardBorder` → `Colors.hairlineBorder` (14 occurrences) across `src/components/pantry/PantryCard.tsx`, `src/components/pantry/SafeSwitchBanner.tsx`, `src/components/pantry/AddToPantryStyles.ts`, `src/components/pantry/SharePantrySheet.tsx`, `src/screens/PantryScreen.tsx`, `src/screens/EditPantryItemScreen.tsx`. `cardSurface` is the same hex (`#242424`) as legacy `card` — purely semantic rename with zero visual delta. `hairlineBorder` is `rgba(255,255,255,0.12)` vs legacy `cardBorder`'s `#333333` — slightly softer; flagged for visual QA. `PantryScreen.tsx:435` uses `cardBorder` as a chip-unselected **background** (not a border), which is the one swap most likely to need review.
  - **C1 — KeyDifference structured snapshot:** New Test 14 in `__tests__/utils/keyDifferences.test.ts` + snapshot file `__tests__/utils/__snapshots__/keyDifferences.test.ts.snap`. Locks the `subject/verb/claim/trailing/text/icon/severity/affectedProduct/id` shape via a realistic 4-rule comparison (unnamed protein A, named meat B, AAFCO B, preservative B). Guards against accidental refactor of the 9 rule emitters or the `buildDiff` helper. Repo has no screen-level UI snapshot infrastructure (no `react-test-renderer`/`@testing-library/react-native`); this is the correct abstraction for the existing helper-extraction testing convention. Full visual snapshot infrastructure intentionally skipped — high cost, low payoff for session 19's rendering-level changes.
  - **CompareScreen card frames (Matte Premium polish):** `ss.section` now has full card anatomy (`backgroundColor: Colors.cardSurface`, `borderRadius: 16`, `padding: Spacing.lg`, `borderWidth: 1`, `borderColor: Colors.hairlineBorder`, `marginBottom: Spacing.md`) so Score Breakdown, Nutrition (DMB), Key Differences, Your Other Pets, and Ingredients all sit inside framed cards (previously floated against the page background — user flagged Ingredients specifically). `ss.productCard` got `borderWidth: 1 / borderColor: hairlineBorder` to match MeScreen's framed style. `ss.diffCard` background shifted from `Colors.card` to `rgba(255,255,255,0.04)` — subtle lift against the new outer `cardSurface` so nested diff cards don't blend. `ss.productHeaders` bottom gap tightened `Spacing.lg → Spacing.md` for consistent card-to-card spacing. CompareScreen legacy token migration: `ss.header`, `ss.bucketRow`, `ss.nutritionRow` borderBottom all migrated from `Colors.cardBorder` → `Colors.hairlineBorder`; `ss.productCard` migrated from `Colors.card` → `Colors.cardSurface`. Full visual spec reference: `.agent/design.md` Card Anatomy section.
  - **Ingredients mini composition bar + severity tally:** New `IngredientsFooter` subcomponent in `CompareScreen.tsx`, rendered at the bottom of each ingredient column. Filters to `position > 10` only (top 10 are listed above — showing them in the bar would be redundant). Renders equal-width severity-colored segments (beyond-top-10 ingredients all share the same position weight of 2 in PositionMap's curve, so equal widths are mathematically accurate). 8px tall × 4px border radius, full-column width, no title/legend/tap interactivity (compact by design). Below the bar: severity tally row with `[colored dot] [count]` pairs in `good → neutral → caution → danger` order; zero-count severities skipped. Component returns `null` entirely when the product has ≤10 ingredients. Reuses existing `ss.severityDot` + local `SEVERITY_DOT` map for color parity with the top-10 dots directly above. Closes the "red/amber hidden beyond top 10" visibility gap — users can now see at a glance whether a product has concerning ingredients past position 10 without expanding anything.
  - **Tests:** 1334 → 1335 passing / 61 suites / 3 snapshots. All green. Scoring untouched, regression anchors verified at handoff (Pure Balance = 62, Temptations = 9).
  - **Files modified:** `src/screens/CompareScreen.tsx`, `src/utils/keyDifferences.ts`, `src/components/pantry/PantryCard.tsx`, `src/components/pantry/SafeSwitchBanner.tsx`, `src/components/pantry/AddToPantryStyles.ts`, `src/components/pantry/SharePantrySheet.tsx`, `src/screens/PantryScreen.tsx`, `src/screens/EditPantryItemScreen.tsx`, `__tests__/utils/keyDifferences.test.ts`, `docs/status/CURRENT.md`
  - **Files added:** `__tests__/utils/__snapshots__/keyDifferences.test.ts.snap`
- **Not done yet:**
  - **Visual QA on iOS simulator** — session 19 compare polish AND session 20 pantry swipe/tokens. Full checklist below under Gotchas. User action: run dev build, walk the checklist. High priority — several visual changes across two sessions need eyeballs.
  - **`PantryScreen.tsx:435` chip-background review** — the `cardBorder → hairlineBorder` swap at that line is the only non-border use (unselected chip background). `rgba(255,255,255,0.12)` over dark background may read too faint vs `#333333`. Revert that single line to a dedicated chip-surface token if it looks broken.
  - **Same-brand disambiguation** for `getConversationalName` — deferred from session 19. Two products sharing brand + identical first-2 descriptor words render identical short names. Flag-later if users hit this in the wild.
  - **Legacy token migration on remaining screens** — HomeScreen, ResultScreen, MeScreen, etc. still use `Colors.card` / `Colors.cardBorder`. Pantry and CompareScreen are now done.
  - **Stale browse scores fix** — CategoryBrowseScreen cache maturity check isn't form-aware (freeze-dried and other minority forms never get scored). Fix needed in both Edge Function and `batchScoreOnDevice.ts`.
  - All prior M9 items carry over: custom icon rollout (5 pending v2 bold), `IconPlatter` component, Kiba Index e2e testing, affiliate enrollment, brand filter on browse, chip badge counts for non-Daily-Food sub-filters, Top Picks per-category dedicated screen, HomeScreen visual overhaul, search UX overhaul.
- **Next session should:** Walk the visual QA checklist below on iOS simulator — session 19 + session 20 changes. Fix the `PantryScreen.tsx:435` chip background if it reads wrong. Then tackle either stale browse scores (higher impact, larger scope) or continue legacy token migration on HomeScreen/ResultScreen/CompareScreen (mechanical, lower risk).
- **Gotchas / Visual QA checklist for next session:**
  - **CompareScreen (session 19 rendering changes):**
    - [ ] `ProductHeader` images sit on an 84px white `productImageStage` (mirrors `TopPicksCarousel`)
    - [ ] Verdict caption below score ring reads `"[Excellent|Good|Fair|Low|Poor] match for [Name]"` — never `"match for your"`
    - [ ] With no active pet (unusual path): caption reads `"[tier] match"` with no trailing name — **A1 fix lands this case**
    - [ ] Key Differences cards: `subject` + `claim` are bold, `verb` + `trailing` are muted
    - [ ] Score Breakdown heatmap: winner cyan, loser dimmed to `textSecondary` (not full white)
    - [ ] Nutrition DMB rows: bold-only differentiation, **no color tint** (D-094 clinical-copy rule)
    - [ ] Center column labels in Nutrition/Score Breakdown use `textTertiary`
    - [ ] Ingredient list: no `1., 2., 3.` numeric prefixes — severity dot is the only ordering cue
    - [ ] Tab bar hidden on CompareScreen, returns on navigate-back
    - [ ] CTA buttons use `getConversationalName()` short-form names (≤34 chars)
    - [ ] Card header product name still uses `stripBrandFromName` with `numberOfLines={2}` — **intentional, not a leftover** (session 19 gotcha)
  - **Pantry (session 20 changes):**
    - [ ] Swipe left on a pantry card → red Delete button → tap → existing confirmation (Alert for single-pet, sheet for shared) → delete
    - [ ] Swipe right on a pantry card → cyan Edit button → tap → navigate to EditPantryItem (or RecallDetail for recalled items)
    - [ ] All pantry surfaces (`PantryScreen`, `EditPantryItemScreen`, `AddToPantrySheet`, `SharePantrySheet`, `SafeSwitchBanner`, `PantryCard`) look visually identical to before the token swap — `cardSurface` is the same hex as legacy `card`
    - [ ] `PantryScreen.tsx:435` filter chip unselected background reads as a visible muted chip (not invisible) after `cardBorder → hairlineBorder` swap. **Revert that one line if it looks broken.**
  - **CompareScreen card frames + mini composition bar (session 20 continuation):**
    - [ ] Every top-level section on CompareScreen (Score Breakdown, Nutrition (DMB), Key Differences, Your Other Pets, Ingredients) sits inside a framed `cardSurface` card with rounded corners and a `hairlineBorder`. Ingredients was the user's primary gripe — verify it's no longer floating.
    - [ ] Product header cards at the top also have a visible frame (`borderWidth: 1 / borderColor: hairlineBorder`) matching MeScreen style.
    - [ ] Key Differences inner diff cards are subtly lifted (`rgba(255,255,255,0.04)`) against the outer section card — no blending.
    - [ ] Below each ingredient column's top 10 list, a mini severity bar renders for any ingredients at position > 10. Bar hides entirely when a product has ≤10 ingredients.
    - [ ] Below the mini bar, a severity tally row shows `[dot] [count]` for each non-zero severity in good → neutral → caution → danger order (e.g. `● 10   ● 3   ● 2` for 10 neutral / 3 caution / 2 danger).
    - [ ] Tally dot colors match the top-10 ingredient dots directly above (local `SEVERITY_DOT` map — single source for dot color on this screen).
    - [ ] All pre-existing CompareScreen visual polish from session 19 still renders correctly (verdict caption, bolded Key Diff, tab bar hide, etc.) — no regressions from the card wrapping.
  - **Code gotchas carrying over from session 19:**
    - `getConversationalName` cap is **34 chars**, not 30 — admits "Brand Foo & Bar" patterns. JSDoc and tests reference 34.
    - `KeyDifference.text` is auto-computed by `buildDiff(parts)`. Don't set `text` manually on new rules. Tests should prefer `.subject`/`.claim` over `.text`.
    - CompareScreen uses `stripBrandFromName` at its card-header 2-line product name — **intentional**, not a leftover.
    - Scoring engine was **not touched** this session. Regression anchors (Pure Balance = 62, Temptations = 9) still valid.
  - **Session 20 specifics:**
    - The `deleteConfirmMessage` prop on `SwipeableRow` is **intentionally omitted** in the pantry wiring — `handleRemove` owns confirmation (Alert for single-pet, multi-pet sheet for shared) and double-confirmation would be user-hostile. Haptic fires slightly before the existing dialog. If future work wants SwipeableRow-native confirmation, it must unify the shared/single branching.
    - Token migration used `replace_all` with `Colors.card,` (trailing comma) to distinguish from `Colors.cardBorder` and `Colors.cardSurface`. All pantry occurrences had the trailing comma, confirmed via grep post-edit. Future migrations on other screens should check trailing punctuation before using `replace_all` on `Colors.card`.
    - `IngredientsFooter` inlines its own position-weight logic (beyond-top-10 = equal width) rather than importing from `PositionMap.tsx`. Keeps CompareScreen self-contained; if you want to share, export `getIngredientPositionWeight` from PositionMap and refactor both call sites.
    - The severity tally uses `good → neutral → caution → danger` order deliberately — stable ordering per column so the eye learns the pattern. Do not re-order for "show the bad stuff first"; that creates visual instability when comparing two products where A has only good/neutral and B has caution/danger.
    - `IngredientsFooter` filters `i.position > 10` (strict greater than, not `>=`). Position 10 is the 10th ingredient and lives in the list above. Position 11+ is what the bar represents.
- **Decision/scoring changes:** None. No new decisions. No scoring logic changed.
